# DisenoSoftware-Entrega-final
Para levantar la aplicación:
- Docker debe estar corriendo en la computadora
- Abrir terminal en la raíz del proyecto (donde está el docker-compose) y ejecutar docker-compose --env-file .env up -d
- Dirigirse a http://localhost:3000/
- Contraseña para pacientes = @Paciente123 y para funcionarios de salud = @Funcsalud123
